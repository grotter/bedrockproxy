{
  "name": "bedrockproxy",
  "version": "1.0.0",
  "type": "module",
  "description": "AWS Bedrock Proxy for OpenAI-compatible API",
  "main": "index.mjs",
  "scripts": {
    "start": "npm run profiles && node local-server.mjs",
    "dev": "npm run profiles && node local-server.mjs",
    "profiles": "aws bedrock list-inference-profiles --no-paginate > inference-profiles.json"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "dotenv": "^17.3.1"
  }
}
